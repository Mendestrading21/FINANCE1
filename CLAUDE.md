# Finance — reprise Claude

Commencer par `AGENTS.md`, `.claude/skills/finance/SKILL.md`, `docs/STATUS.md`, `docs/PLAN.md`, puis `docs/AUDIT_UI_V2.md` et `docs/PLAN_AMELIORATION_V2.md` pour la tranche actuelle.

Le nom de l’application est Finance et le dépôt canonique est `Mendestrading21/Finances`. Le dépôt initialement appelé Finance1 contenait un suivi d’habitudes, désormais archivé dans `legacy/`. Le code, la branche, le site Pages et les preuves actuelles déterminent l’avancement.

Les sous-agents sont décrits dans `.claude/agents/finance-*.md`. Les utiliser pour les lots qui peuvent avancer indépendamment et pour la relecture des changements sensibles. Si l’environnement n’offre pas de sous-agents, exécuter le travail disponible et expliciter la limite de revue.

Le coffre local utilise Web Crypto et un export chiffré. Les données Notion réelles restent hors Git. Ne pas remplacer un manque d’accès Notion par des chiffres de mémoire et ne pas annoncer de cloud automatique non configuré.

Continuer dans le périmètre déjà autorisé : développer, tester, corriger et publier le code dans `Mendestrading21/Finances` avec les protections en place. GitHub Pages est la cible de cette PWA ; vérifier le workflow et l’URL après publication. Ne pas activer de service payant ou de connexion bancaire. Ne jamais accorder de permissions supplémentaires pour contourner un blocage.

Voir `DEMARRER_CLAUDE.md` pour le prompt de reprise, `docs/RECHERCHE_OUTILS.md` pour le socle et `docs/RECHERCHE_UI_V2.md` pour les choix de la refonte compacte.
